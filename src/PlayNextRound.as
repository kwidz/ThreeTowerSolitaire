package  
{
	import flash.display.DisplayObject;
	import flash.display.SimpleButton;
	
	/**
	 * ...
	 * @author geoffrey glangine
	 */
	public class PlayNextRound extends SimpleButton 
	{
		
		public function PlayNextRound(upState:DisplayObject=null, overState:DisplayObject=null, downState:DisplayObject=null, hitTestState:DisplayObject=null) 
		{
			super(upState, overState, downState, hitTestState);
			
		}
		
	}

}